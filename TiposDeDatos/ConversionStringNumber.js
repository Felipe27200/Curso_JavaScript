let cadenaNumero = "10";

console.log(cadenaNumero);
console.log(typeof cadenaNumero);

// Usar Number() para convertir la cadena en number
let numero = Number(cadenaNumero);

console.log(numero);

// Tipo de dato de cada variable
console.log(typeof cadenaNumero)
console.log(typeof numero)

// Comprobar si un valor no es un número
console.log(isNaN(cadenaNumero))